name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main  # 默认分支

# 设置 GITHUB_TOKEN 的权限
permissions:
  contents: read
  pages: write
  id-token: write

# 允许一个并发部署
concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Create necessary directories and files
        run: |
          # 创建目录
          mkdir -p .intlayer/main
          mkdir -p .intlayer/dictionary
          mkdir -p src/locales
          
          # 创建 app.json 文件
          cat > .intlayer/dictionary/app.json << 'EOL'
          {
            "key": "app",
            "content": {
              "nav": {
                "home": {
                  "nodeType": "translation",
                  "translation": {
                    "zh-Hans": "首页",
                    "en": "Home",
                    "ja": "ホーム"
                  }
                },
                "articles": {
                  "nodeType": "translation",
                  "translation": {
                    "zh-Hans": "文章",
                    "en": "Articles",
                    "ja": "記事"
                  }
                }
              }
            }
          }
          EOL
          
          # 创建 dictionaries.cjs 文件
          cat > .intlayer/main/dictionaries.cjs << 'EOL'
          'use strict';
          const app = require('../dictionary/app.json');
          module.exports = { app };
          EOL
          
          # 创建 dictionaries.mjs 文件
          cat > .intlayer/main/dictionaries.mjs << 'EOL'
          import app from '../dictionary/app.json' assert { type: 'json' };
          export default { app };
          EOL
          
          # 创建其他必要的文件
          cat > .intlayer/main/dynamic_dictionaries.cjs << 'EOL'
          'use strict';
          module.exports = {};
          EOL
          
          cat > .intlayer/main/dynamic_dictionaries.mjs << 'EOL'
          export default {};
          EOL
          
          cat > .intlayer/main/unmerged_dictionaries.cjs << 'EOL'
          'use strict';
          module.exports = {};
          EOL
          
          cat > .intlayer/main/unmerged_dictionaries.mjs << 'EOL'
          export default {};
          EOL

          # 显示创建的文件
          echo "Created files:"
          ls -la .intlayer/main/
          ls -la .intlayer/dictionary/
          
          echo "Content of dictionaries.cjs:"
          cat .intlayer/main/dictionaries.cjs
          
          echo "Content of app.json:"
          cat .intlayer/dictionary/app.json

      - name: Build Intlayer configuration
        run: |
          echo "Running Intlayer build..."
          NODE_ENV=production npm run intlayer:build
          echo "Checking generated files:"
          ls -la .intlayer/
          ls -la .intlayer/main/
          ls -la .intlayer/dictionary/

      - name: Build project
        run: |
          echo "Starting project build..."
          NODE_ENV=production npm run build
          echo "Checking build output:"
          ls -la dist/

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './dist'

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4 